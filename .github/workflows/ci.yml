name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install minimal runtime deps
        run: |
          python -m pip install --upgrade pip
          pip install "fastapi==0.116.1" "uvicorn[standard]==0.30.1" "pydantic==2.8.2" "SQLAlchemy==2.0.32"

      - name: Show tree (debug)
        run: |
          pwd
          ls -la
          ls -la app

      - name: Smoke test (import app)
        run: python -c "import app.main; print('import-ok')"
